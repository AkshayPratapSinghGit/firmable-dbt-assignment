version: 2

sources:
  - name: raw
    schema: raw

    tables:
      - name: yellow_tripdata
        description: Raw Yellow Taxi trips

        freshness:
          warn_after:
            count: 1
            period: day

      - name: taxi_zone_lookup
        description: Taxi zone lookup table